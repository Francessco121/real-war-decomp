name: pe_coff
description: Native Dart parser for Windows Portable Executable (PE) and Common Object File Format (COFF) files.
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dev_dependencies:
  file: ^7.0.0 # for mocking file IO
  lints: ^3.0.0
  test: ^1.25.1
