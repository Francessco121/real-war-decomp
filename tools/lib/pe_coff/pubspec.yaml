name: pe_coff
description: Native Dart parser for Windows Portable Executable (PE) and Common Object File Format (COFF) files.
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dev_dependencies:
  file: ^6.1.4 # for mocking file IO
  lints: ^2.0.0
  test: ^1.24.1
